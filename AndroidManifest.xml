<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".friendRequestList"></activity>
        <activity android:name=".friendRequestSent" />
        <activity android:name=".friendList" />
        <activity android:name=".showingOtherUsers" />
        <activity android:name=".completePage" />
        <activity android:name=".favouriteCableNetwork" />
        <activity android:name=".favouriteSport" />
        <activity android:name=".setNameOfUsers" />
        <activity android:name=".howOldAreYou" />
        <activity android:name=".login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>